git clone (url)					updating a text file
git add (file names)				git commit -m"" file name
git commit -m "(committed message)		git push -u origin master
git push -u origin master
git remote add origin master (url)

		to upload the whole file
git init
git add .
git commit -m ""
git status
git add origin url
git push -f origin master
